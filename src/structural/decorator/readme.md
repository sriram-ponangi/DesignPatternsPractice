## Definition:
- Decorator design pattern allows the user to add new functionality 
to and existing object without altering its structure.
- In this pattern we create a decorator class
  that acts as a wrapper to the existing class.
  